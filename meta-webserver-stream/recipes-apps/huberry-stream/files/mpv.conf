# System-wide mpv config for my-retro
# Point libass directly at the DejaVu font so subtitles and OSD work
# without requiring fc-cache to have been run on the target.
sub-font=/usr/share/fonts/truetype/DejaVuSans.ttf
osd-font=/usr/share/fonts/truetype/DejaVuSans.ttf
