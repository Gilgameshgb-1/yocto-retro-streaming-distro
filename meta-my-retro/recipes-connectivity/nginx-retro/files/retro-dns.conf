# Custom DNS entries for the retro Pi hotspot.
# Placed in NetworkManager's dnsmasq.d so the hotspot's built-in dnsmasq
# instance resolves these names for connected WiFi clients.
address=/retro.pi/10.42.0.1
address=/stream.pi/10.42.0.1
